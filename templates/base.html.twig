<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="Renault_21"/>
        <meta name="author" content="Richard"/>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        <link rel="stylesheet" href="{{ asset('asset/css/style.css') }}">
        {%  block style %}{% endblock %}
        <title>{% block title %}Renault 21{% endblock %}</title>
    </head>
    <body>
    <header>
        <nav>
            <ul class="lien">
                <a href="{{ path('base') }}">
                <li class="logoDuSite"><img src="{{asset('images/LogoDuSite.png')}}" alt="logo"></li></a>
                <li id="pieceHeader"><a href="{{ path('piece.ajouter') }}" alt="pièces détachées de véhicule">PIECES DETACHEES</a></li>
                <li><a href="{{ path('user.inscription') }}" alt="Faîtes votre inscription">INSCRIPTION</a></li>
                <li><a href="{{ path('user.message') }}" alt="laisser un message">MESSAGE</a></li>
                <li><a href="{{ path('app_login') }}" alt="Connectez-vous">CONNEXION</a></li>
                <li><a href="{{ path('app_logout') }}" alt="Déconnectez-vous">DECONNEXION</a></li>
            </ul>
        </nav>
    </header>
    <main>
    {% for message in app.flashes('success') %}
        <h2 id="flashSuccess">{{ message }}</h2>
    {% endfor %}

    {% for message in app.flashes('connexion') %}
        <h2 id="flashSuccess">{{ message }}{{ user.name }}</h2>
    {% endfor %}

    {% for message in app.flashes('fail') %}
        <h2 id="flashFail">{{ message }}</h2>
    {% endfor %}
    {% block body %}{% endblock %}
    </main>
    <footer>
        <nav>
<ul class="lien">
    <li><a href="{{ 'regle.regleDeSecurité' }}" alt="consulter les règles de sécurité">Règle de sécurité</a></li>
    <li id="pieceFooter"><a href="{{ path('piece.ajouter') }}" alt="Ajouter des pièces">PIECES DETACHEES</a></li>
    <li><a href="{{ 'regle.mentionsLegales' }}" alt="Consulter les mentions légales">Mentions Légales</a></li>
    <li><a href="{{ 'regle.copyright' }}" alt="Consulter le copyright">Copyright</a></li>
</ul>
        </nav>
    </footer>
    <script src="js/index.js"></script>
    </body>
</html>
